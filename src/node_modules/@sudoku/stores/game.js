import { writable, derived } from 'svelte/store';
import gameStore from '../../../domain/gameStore';

export const gamePaused = writable(true);

export const gameWon = derived(
	gameStore,
	($gameStore) => {
		return $gameStore.won;
	},
	false
);